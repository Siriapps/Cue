"""Real-time keyword extraction from audio (deprioritized). Returns keywords for display when avatar/real-time UX is resumed."""
from typing import List


def extract_keywords_from_audio(audio_base64: str, mime_type: str) -> List[str]:
    """Extract keywords from audio chunk. Stub: returns empty list until real-time UX is enabled."""
    return []
